import styled from "styled-components";

const ButtonsGroup = styled.div`
  width: 100%;  
  display: flex;
  justify-content: flex-end;
`;

const Title = styled.h2`
  margin: 0;
`;

export const Styled = { ButtonsGroup, Title };

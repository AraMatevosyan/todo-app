import styled from "styled-components";

const ButtonsGroup = styled.div`
  width: 100%;
  display: flex;
  justify-content: flex-end;
  gap: 10px;
`;

const Title = styled.h2`
  margin: 0;
`;

const Content = styled.div`
  margin: 10px 0;
`;

export const Styled = { ButtonsGroup, Title, Content };
